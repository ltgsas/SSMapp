<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--
    这些属性可以在外部进行配置，并可以进行动态替换。
    你既可以在典型的 Java 属性文件中配置这些属性，也可以在 properties 元素的子元素中设置。
    -->
    <properties resource="jdbc.properties"/>
    <!--设置日志输出-->
    <settings>
        <!--标准日志值'STDOUT_LOGGING',可以不用配置另的信息-->
        <setting name="logImpl" value="STDOUT_LOGGING"/>
    </settings>
    <!-- 类型别名可为 Java 类型设置一个缩写名字。 它仅用于 XML 配置，意在降低冗余的全限定类名书写。-->
    <typeAliases>
        <!-- 批量实体别名注册:别名是类名的驼峰命名法(规范) -->
        <package name="com.ltgsas.mybatis.pojo"/>
    </typeAliases>
    <!--    配置分页插件-->
    <plugins>
        <!-- com.github.pagehelper为PageHelper类所在包名 -->
        <plugin interceptor="com.github.pagehelper.PageInterceptor">
            <!-- 使用下面的方式配置参数，后面会有所有的参数介绍 -->
            <property name="helpDialect" value="mysql"/>
            <property name="rowBoundsWithCount" value="false"/>
        </plugin>
    </plugins>
    <!--配置环境变量-->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <!--
        注册　mapper.xml 文件,推荐使用 resource 方式: 从 resource 目录下找指定的文件加载;
          使用 url/class 有限制: 1.接口及其 mapper配置文件必须"同名"并且"在同一个包下".
        -->
        <mapper resource="com/ltgsas/mybatis/mapper/CasebaseMapper.xml"/>
        <!--        <mapper resource="com/ltgsas/mybatis/mapper/CasescoreMapper.xml"/>-->
        <mapper resource="com/ltgsas/mybatis/mapper/TdbassistanMapper.xml"/>
    </mappers>
</configuration>